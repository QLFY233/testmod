# Disable autocrlf on generated files, they always generate with LF
# Add any extra files or paths here to make git stop saying they
# are changed when only line ending changes.
src/generated/**/.cache/* text eol=lf
src/generated/**/*.json text eol=lf

# The Gradle wrapper and shell scripts run on the Linux CI runner.
# If they get CRLF line endings, the build fails with "bad interpreter: /bin/sh^M".
gradlew text eol=lf
*.sh text eol=lf
